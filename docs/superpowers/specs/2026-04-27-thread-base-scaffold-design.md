# thread-base — Initial Scaffold Design

**Date:** 2026-04-27
**Status:** Approved
**Scope:** Initial scaffold of an empty Vue 3 + TypeScript + Bun + Supabase project. No application features.

## Goals

Create a clean, modern, full-stack-ready Vue project named `thread-base` with:

- Vue 3 + TypeScript + Vite + Bun
- Tailwind CSS v4
- oxlint + Prettier
- Vitest
- Supabase: CLI-based local stack (Docker), `@supabase/supabase-js` client, generated TypeScript types committed to the repo
- Auth **infrastructure** only — Supabase client singleton, Pinia auth store, `useAuth()` composable, `requireAuth` router guard. **No login views.**

Future-compatible with Capacitor (mobile apps) — design must not preclude that addition.

## Non-Goals

- No login/signup/password-reset views (later)
- No application domain logic, schema, or features (later)
- No Capacitor integration in this scaffold (later, but design accommodates it)
- No CI/CD, no Docker container for the app, no deployment config
- No Storybook, no Playwright

## Stack

| Concern | Choice | Notes |
|---|---|---|
| Build tool | Vite | Bun-compatible; standard Vue tooling |
| Package manager + runtime | Bun | `bun install`, `bun run …` |
| UI framework | Vue 3 | Composition API, `<script setup>` |
| Routing | Vue Router 4 | `createWebHistory` initially (see Capacitor note below) |
| State | Pinia | Composition-style stores |
| Styling | Tailwind CSS v4 | `@tailwindcss/vite` plugin (no PostCSS config) |
| Linting | oxlint | Rust-based, much faster than ESLint; Vue + TS plugins |
| Formatting | Prettier | No ESLint integration needed (oxlint stays out of formatting) |
| Tests | Vitest + `@vue/test-utils` | jsdom environment |
| Backend client | `@supabase/supabase-js` | Typed via generated `Database` interface |
| Backend tooling | Supabase CLI | Local Docker stack, versioned migrations |

**Explicitly excluded (from scaffold):** Nuxt (no SSR needed), `@supabase/auth-ui-vue` (user wants to build UI later), Playwright, Storybook.

## Project Structure

```
thread-base/
├── .env.example
├── .env.local                    # gitignored
├── .gitignore
├── .oxlintrc.json
├── .prettierrc
├── CLAUDE.md                     # project guidance for Claude (stack, conventions, commands)
├── index.html
├── package.json
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.node.json
├── vite.config.ts
├── vitest.config.ts
├── README.md
│
├── src/
│   ├── main.ts                   # bootstraps Pinia, Router, awaits auth init before mount
│   ├── App.vue
│   ├── App.spec.ts               # smoke test
│   ├── assets/
│   │   └── main.css              # @import "tailwindcss";
│   │
│   ├── lib/
│   │   └── supabase.ts           # typed client singleton
│   │
│   ├── stores/
│   │   └── auth.ts               # Pinia: user, session, loading, initialize, signOut
│   │
│   ├── composables/
│   │   └── useAuth.ts            # ergonomic wrapper over auth store
│   │
│   ├── router/
│   │   ├── index.ts              # router definition
│   │   └── guards.ts             # requireAuth navigation guard
│   │
│   ├── types/
│   │   └── database.ts           # generated by `bun run db:types` (committed)
│   │
│   └── views/
│       └── HomeView.vue          # single placeholder view
│
└── supabase/                     # created via `bunx supabase init`
    ├── config.toml
    ├── migrations/               # versioned SQL
    └── seed.sql
```

**Conventions:**

- `lib/` — external service clients (Supabase only for now)
- `composables/` — Vue-reactive helpers
- `stores/` — Pinia state
- Generated DB types live in `src/types/database.ts` and **are committed** (no codegen step in CI; reviewable in diffs)

## Auth Architecture

Three layers, cleanly separated:

### `src/lib/supabase.ts` — Client Singleton

```ts
import { createClient } from '@supabase/supabase-js'
import type { Database } from '@/types/database'

export const supabase = createClient<Database>(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY,
)
```

A single client for the whole app. The `Database` generic types `.from('table')` calls automatically.

### `src/stores/auth.ts` — Pinia Store

State:

- `user: User | null`
- `session: Session | null`
- `loading: boolean` — true until `initialize()` completes the first time

Actions:

- `initialize()` — calls `supabase.auth.getSession()` to populate state, then subscribes to `supabase.auth.onAuthStateChange()` to keep state in sync. Idempotent (safe to call multiple times — only subscribes once).
- `signOut()` — wraps `supabase.auth.signOut()`.

Getters:

- `isAuthenticated` — `!!user && !!session`

### `src/composables/useAuth.ts` — Component-facing API

```ts
export function useAuth() {
  const store = useAuthStore()
  return {
    user: computed(() => store.user),
    session: computed(() => store.session),
    isAuthenticated: computed(() => store.isAuthenticated),
    signOut: store.signOut,
  }
}
```

Components import `useAuth()`, never `useAuthStore()` directly. The store is an internal implementation detail; the composable is the public API.

### `src/router/guards.ts` — `requireAuth`

- If `auth.loading` is true, await its completion (resolves once `initialize()` finishes).
- If `!auth.isAuthenticated`, redirect to `/login` with `redirect=<originalPath>` query param so post-login navigation can return.
- The `/login` route exists in `router/index.ts` but its component is a stub `<div>Login coming soon</div>` for now.

### `main.ts` — Bootstrap Order

```ts
const app = createApp(App)
app.use(createPinia())
const auth = useAuthStore()
await auth.initialize()       // critical: BEFORE router + mount
app.use(router)
app.mount('#app')
```

`initialize()` must complete before mount so the first route render sees correct auth state. This avoids flicker and a race condition where `requireAuth` fires before the session is loaded.

## Capacitor Future-Compatibility

Capacitor (mobile apps) will be added later. To avoid painting ourselves into a corner:

- **Routing:** `createWebHistory` for now. When Capacitor is added, switch to `createWebHashHistory` or use Capacitor's deep-link config — decision deferred.
- **No Node-only APIs in `src/`** — everything must run in browser/WebView. Currently no risk; document as a constraint.
- **Auth redirect URLs:** Mobile will need a custom URL scheme (e.g., `app.threadbase://auth/callback`) instead of `https://`. Today nothing hardcodes `window.location.origin`, so the future change is localized.
- **Asset paths:** Use Vite's `base` config for relative asset paths (default is fine for now).

These are notes, not work items for this scaffold.

## Tooling & Scripts

### Supabase CLI

Installed via Homebrew (`brew install supabase/tap/supabase`), not as an npm dep. README documents this.

Versioned in `supabase/`:

- `config.toml` — local stack config (ports, auth settings)
- `migrations/*.sql` — schema versioning
- `seed.sql` — test data for `db reset`

Gitignored: `supabase/.branches`, `supabase/.temp`.

### `package.json` scripts

| Script | Command | Purpose |
|---|---|---|
| `dev` | `vite` | Dev server with HMR |
| `build` | `vue-tsc -b && vite build` | Type-check + production build |
| `preview` | `vite preview` | Serve production build locally |
| `test` | `vitest` | Unit tests (watch) |
| `test:run` | `vitest run` | Unit tests (one-shot, for CI) |
| `lint` | `oxlint` | Lint |
| `lint:fix` | `oxlint --fix` | Auto-fix |
| `format` | `prettier --write .` | Format |
| `typecheck` | `vue-tsc --noEmit` | Type-check only |
| `db:start` | `supabase start` | Start local Supabase stack (Docker) |
| `db:stop` | `supabase stop` | Stop local stack |
| `db:status` | `supabase status` | Show ports + keys |
| `db:reset` | `supabase db reset` | Recreate DB from migrations + seed |
| `db:diff` | `supabase db diff` | Generate migration; pass name as arg, e.g. `bun run db:diff -- -f my_migration` |
| `db:types` | `supabase gen types typescript --local > src/types/database.ts` | Regenerate DB types |
| `db:link` | `supabase link` | Link remote; pass ref as arg, e.g. `bun run db:link -- --project-ref abc123` |
| `db:push` | `supabase db push` | Push migrations to remote |

### Env vars

```
.env.example          # committed — documents required vars
.env.local            # gitignored — actual values
```

`.env.example`:

```
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<from `bun run db:status`>
```

The local anon key is printed by `supabase start` / `supabase status`. Same vars apply for remote (different values).

## Lint / Format / Test Config

### oxlint — `.oxlintrc.json`

```json
{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["typescript", "vue", "import"],
  "categories": { "correctness": "error", "suspicious": "warn" },
  "ignorePatterns": ["dist", "node_modules", "src/types/database.ts"]
}
```

Generated DB types are excluded so lint output stays clean.

### Prettier — `.prettierrc`

```json
{
  "semi": false,
  "singleQuote": true,
  "printWidth": 100,
  "trailingComma": "all"
}
```

oxlint does not handle formatting, so no `eslint-config-prettier`-style integration is needed.

### Vitest — `vitest.config.ts`

- `environment: 'jsdom'` (Vue components testable)
- `globals: true` (no boilerplate imports for `describe` / `it`)
- Plugins: same Vue + tsconfig-paths plugins as the dev config
- One smoke test in `src/App.spec.ts` that mounts `<App />` and asserts no error

### TypeScript

Standard Vue 3 setup with project references:

- `tsconfig.json` (root, only `references`)
- `tsconfig.app.json` — app code, `lib: ["ES2022", "DOM"]`, path alias `"@/*": ["./src/*"]`
- `tsconfig.node.json` — Vite config and other Node-context files

Path alias is shared with Vite via `vite-tsconfig-paths` (single source of truth).

### Vite — `vite.config.ts`

Plugins: `@vitejs/plugin-vue`, `@tailwindcss/vite`, `vite-tsconfig-paths`, `vite-plugin-vue-devtools`.

### `.gitignore` highlights

```
node_modules
dist
.env.local
.env*.local
supabase/.branches
supabase/.temp
*.log
.DS_Store
```

## CLAUDE.md

A `CLAUDE.md` at the repo root captures project-specific guidance Claude needs when working in this codebase. It is intentionally separate from `README.md` (developer-facing) and the spec (decision log).

Contents:

- **Stack summary** — locked-in choices (Vue 3, Bun, oxlint not ESLint, Tailwind v4 not v3, Composition API only)
- **Commands cheat sheet** — every `bun run …` script and what it does
- **Architectural rules** — `lib/` for service clients, `composables/` for Vue helpers, `stores/` for Pinia; components use `useAuth()` not `useAuthStore()` directly; auth must be initialized before mount
- **Path alias** — `@/*` → `src/*`
- **DB workflow** — migrations live in `supabase/migrations/`, regenerate types after schema changes
- **Future constraints** — Capacitor planned: no Node-only APIs in `src/`, redirect URLs must stay configurable
- **Tooling guardrails** — prefer newest stable tools; don't suggest ESLint/npm/yarn/Tailwind v3/Options API

## README

Setup steps:

1. Install Bun (`curl -fsSL https://bun.sh/install | bash`)
2. Install Supabase CLI (`brew install supabase/tap/supabase`)
3. `bun install`
4. `bun run db:start` — first run downloads Docker images
5. Copy printed keys into `.env.local`
6. `bun run db:types` — generates DB types
7. `bun run dev`

Plus a "Coming later" note for Capacitor.

## Open Questions

None at scaffold time. Future scaffolding-adjacent decisions (deferred):

- Login/signup view styling and flow (Magic Link vs Email/Password vs OAuth)
- Capacitor integration approach (router history mode, deep links, auth redirect scheme)
- CI/CD pipeline
- First domain schema + migrations